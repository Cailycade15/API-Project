export type Todo = {
    id: number,
    title: string,
    description: string,
    completed: string
  }

export type Todo_Add = {
    title: string,
    description: string,
    completed: string
  }
